language: node_js
node_js:
  - 8
# keep the npm cache around to speed up installs (see https://docs.npmjs.com/cli/ci)
cache:
  directories:
    - "$HOME/.npm"
before_install:
  - "cd optaweb-tsp-ui"
script:
  - npm run build
  - npm run lint
  - npm test
